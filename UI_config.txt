;--temp
[UI Settings]

; Размер текста метаданных, рекомендуемое значение 12-13, по стандарту 12
text_size=12

; Закругление обложки трека, по стандарту 16. Если ставить больше чем b_radius, будет видно уголки
radius = 16
;--

;ЗАДНИКИ
[UBOX Settings]

; Закругление рамки UBOX, по стандарту 16
b_radius = 16

[LBOX Settings]

; Закругление рамки LBOX, по стандарту 16
b_radius = 16

[CBOX Settings]

; Закругление рамки CBOX, по стандарту 16
b_radius = 16

[RBOX Settings]

; Закругление рамки RBOX, по стандарту 16
b_radius = 16

[DBOX Settings]

; Закругление рамки DBOX, по стандарту 16
b_radius = 16




[UI adress_bar] ; точечная настройка дизайна адресной строки

; ВАЖНО у HEX цветов формат #6A5ACD - 6 символов после #, если в вашем HEX цвете 8 символов (#BB5A225C), то 
; последние 2 отвечают за прозрачность, у меня прозрачность задается отдельно так что их нужно УБРАТЬ


; Цвет фона строки в #HEX формате (16рич.)
adress_barBGCol = #3c3e40

; Прозрачность фона строки <=1.00
adress_barBGOp = 0.9

; Цвет рамки (обводки) строки
adress_barBorderCol = #ededed

; Прозрачность рамки строки <=1.00
adress_barBorderOp = 0.4

[UI adress_button] ; точечная настройка дизайна кнопки выбора файла

; Цвет иконки кнопки выбора файла справа от строки
adress_ButtonIconCol = #ffffff

; Цвет фона кнопки выбора файла справа от строки
adress_ButtonBGCol = #146ba6

; Цвет рамки (обводки) кнопки выбора файла справа от строки
adress_Button_BCol = #ededed

; Толщина рамки (обводки) кнопки выбора файла справа от строки
adress_Button_BTol = 1.5

; Сила скругления углов кнопки выбора файла справа от строки
adress_Button_Radius = 8

[UI search_bar] ; точечная настройка дизайна кнопки выбора файла

; Цвет фона строки поиска
search_barBGCol = #3c3e40

; Прозрачность фона строки поиска <=1.00
search_barBGOp = 0.9

; Цвет рамки (обводки) строки поиска
search_barBorderCol = #ededed

; Прозрачность рамки строки поиска <=1.00
search_barBorderOp = 0.4

; Сила скругления углов строки поиска, по стандарту 8
search_bar_radius = 8

[UI queue_bar] ; настройка размера и цвета ячеек очереди

; Скругление ячейки (целое число). по стандарту 8
queue_border_radius = 8

; Use autopresets/Use presets/Customise all

; Поставьте цифру в соответствии используемой стратегии настройки размера ячеек:
; Автопресеты - 0, пресеты - 1, собственная настройка - 2.
method_queue_settings = 0

; Autopersets: автопресеты вычисляются по формуле:
; (46 * k + 8.2), (12 * k + 5.4), (12 * k + 2.4), (4 * k - 1.2), (15 * k - 4), (76 * k + 4.2)
; Где каждый блок соответствует следующим параметрам:
; (размер обложки, размер названия, размер автора, расстояние между ними, отступы внутри ячейки, высота ячейки)
; От пользователя требуется указать аргумент k где k (0.0-1.0). По стандарту указывается 0.7
k_queue = 0.7

; Presets: Пресеты: всего 4 штуки, от меньшего к большему. Введите цифру (от 0 до 3)
queue_cell_preset = 0
; Исходные данные пресетов(для ваших нужд):

;       match queue_cell_preset:
;           case 0:
;               queue_cell = (22, 9, 6, 0, 0.5, 27)
;           case 1:
;               queue_cell = (28, 11, 8, 0, 1, 33)
;           case 2:
;               queue_cell = (35, 13, 10, 2, 3, 48)
;           case 3:
;               queue_cell = (45, 15, 12, 2, 8, 60)

; Customise: Точечная настройка работает по той же стратегии, что и пресеты. Каждое число (0.0 - 100.0)соответствует своей настройке
; (размер обложки, размер названия, размер автора, расстояние между ними, отступы внутри ячейки, высота ячейки)
queue_cell1 = 28
queue_cell2 = 11
queue_cell3 = 8
queue_cell4 = 0
queue_cell5 = 1
queue_cell6 = 33

[UI track_bar] ; настройка размера и цвета ячеек треков. Система идентична queue_bar

; Скругление ячейки (целое число). по стандарту 8
track_border_radius = 8

; Use autopresets/Use presets/Customise all

; Поставьте цифру в соответствии используемой стратегии настройки размера ячеек:
; Автопресеты - 0, пресеты - 1, собственная настройка - 2.
method_tracks_settings = 0

; Autopersets: автопресеты вычисляются по формуле:
; (46 * k + 8.2), (12 * k + 5.4), (12 * k + 2.4), (4 * k - 1.2), (15 * k - 4), (76 * k + 4.2)
; Где каждый блок соответствует следующим параметрам:
; (размер обложки, размер названия, размер автора, расстояние между ними, отступы внутри ячейки, высота ячейки)
; От пользователя требуется указать аргумент k где k (0.0-1.0). По стандарту указывается 0.65
k_track = 0.65

; Presets: Пресеты: всего 4 штуки, от меньшего к большему. Введите цифру (от 0 до 3)
track_cell_preset = 2
; Исходные данные пресетов(для ваших нужд):

;       match track_cell_preset:
;           case 0:
;               track_cell = (22, 9, 6, 0, 0.5, 27)
;           case 1:
;               track_cell = (28, 11, 8, 0, 1, 33)
;           case 2:
;               track_cell = (35, 13, 10, 2, 3, 48)
;           case 3:
;               track_cell = (45, 15, 12, 2, 8, 60)

; Customise: Точечная настройка работает по той же стратегии, что и пресеты. Каждое число (0.0 - 100.0)соответствует своей настройке
; (размер обложки, размер названия, размер автора, расстояние между ними, отступы внутри ячейки, высота ячейки)
track_cell1 = 28
track_cell2 = 11
track_cell3 = 8
track_cell4 = 0
track_cell5 = 1
track_cell6 = 33